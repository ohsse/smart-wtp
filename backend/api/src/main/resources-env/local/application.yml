spring:
  datasource:
    url: jdbc:p6spy:postgresql://localhost:5432/smartwtp
    username: smartwtp
    password: smartwtp
    driver-class-name: com.p6spy.engine.spy.P6SpyDriver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: false
        show_sql: false

server:
  port: 8080
